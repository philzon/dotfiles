#!/bin/bash

docker run \
    --init \
    --rm \
    -it \
    --user $(id -u):$(id -g) \
    --workdir ${PWD} \
    --volume ${PWD}:${PWD} \
    --env XDG_CACHE_HOME=/tmp/.cache \
    golang:1.12 \
    ${@:-bash}

exit $?
